name: pose_detection_example_web
description: Web example for pose_detection
publish_to: 'none'
version: 1.0.0

environment:
  sdk: '>=3.10.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  pose_detection:
    path: ../
  web: ^1.1.1

dev_dependencies:
  flutter_lints: ^6.0.0

dependency_overrides:
  flutter_litert: ^3.3.1

flutter:
  uses-material-design: true
  assets:
    - assets/samples/
